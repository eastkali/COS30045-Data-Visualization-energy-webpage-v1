# Exercise 4.4
## AI Declaration

AI tools were used for technical concepts, particularly in loading and formatting CSV data using d3.csv(), handling asynchronous data loading with .then(), and correctly converting string values to numeric types for visualisation.

The assistance was mainly used to clarify how data binding and console debugging work in D3, while all code implementation, file setup, and integration into the project were completed independently.